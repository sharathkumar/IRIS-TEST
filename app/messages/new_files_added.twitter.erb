<% define_content :link do -%>
  <%= polymorphic_url([asset.context, :files]) %>
<% end -%>
<%= t('body', 'Iristutor Alert - %{count} new files for %{name}', 
        :count => n(data.count),
        :name => asset.context.name) %>